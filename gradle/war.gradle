apply plugin: "war"

bootWar {
    mainClassName = "com.adventure.uaa.AdventureUaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
